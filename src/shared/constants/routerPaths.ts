const root = ''

export const ROUTER_PATHS = {
    main: root + '/',
    about: root + '#about',
    testimonials: root + '/#testimonials',
    contacts: root + '/#contacts',
    masters: root + '/masters',
}